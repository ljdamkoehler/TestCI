name: Post PR Checklist

on:
  pull_request:
    # types:
    #   - opened

jobs:
  post_checklist:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Post PR Checklist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python post_checklist.py
